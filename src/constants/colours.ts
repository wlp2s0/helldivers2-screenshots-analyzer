import { rgbaToInt } from "jimp";

/**
 * The integer representation of the red colour.
 * @internal
 */
export const red = rgbaToInt(255, 0, 0, 255);

/**
 * The integer representation of the green colour.
 * @internal
 */
export const green = rgbaToInt(0, 255, 0, 255);

/**
 * The integer representation of the blue colour.
 * @internal
 */
export const blue = rgbaToInt(0, 0, 255, 255);

/**
 * The integer representation of the white colour.
 * @internal
 */
export const white = rgbaToInt(255, 255, 255, 255);

/**
 * The integer representation of the yellow colour.
 * @internal
 */
export const yellow = rgbaToInt(255, 255, 0, 255);

/**
 * The integer representation of the cyan colour.
 * @internal
 */
export const cyan = rgbaToInt(0, 255, 255, 255);

/**
 * The integer representation of the magenta colour.
 * @internal
 */
export const magenta = rgbaToInt(255, 0, 255, 255);
